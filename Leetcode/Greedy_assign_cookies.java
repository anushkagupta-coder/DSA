public class Greedy_assign_cookies {
    public static void main(String[] args){

    }

    static int func(int[] greedy , int[] size){
        int a=greedy.length;
        int b= size.length;

        int left=0;
        int right=0;
        Arrays.sort(greed);
        Arrays.sort(size);
        while(left<b){
            if(greed[r]<=size[l]){
                right=right+1;
            }
            left=left+1;
        }
        return r;
    }
}
